package server.game;

import org.junit.jupiter.api.BeforeEach;
import org.mockito.Mockito;

class GameManagerTest {

    GameManager manager;

    @BeforeEach
    void init(){
        INotifyEvent notify = Mockito.mock(INotifyEvent.class);
    //    manager = new GameManager()
    }


}